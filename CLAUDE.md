# DEEP DIVER (shooter)

ビルド不要の単一ファイルCanvasゲーム。`index.html` → `version.js` → `game.js` の順に読み込まれる。

## アーキテクチャ境界

- 全ロジックは `game.js` 内の単一IIFE（`(() => { 'use strict'; ... })();`）に閉じる。新しいファイルへの分割、モジュールシステム、bundler/transpilerの導入はしない
- `game.js` 内部は `// ---------- セクション名 ----------` というコメントで区切られたセクション構成（ハイスコア／サウンド／ゲーム状態／面構成／入力／地形／自機／弾／敵／ボス／アイテム／衝突判定／描画／メインループ 等）。新規コードは対応するセクションに置く
- `version.js` は `.github/workflows/version-bump.yml` がPRの作成・更新のたびに自動で書き換える生成ファイル。**手動で編集しない**

## 命名規約

- 関数は動詞プレフィックスで揃える: `drawX`（描画）/ `updateX`（毎フレーム更新）/ `spawnX`（生成）/ `collidesX`（当たり判定）/ `resetX`（ステージ・状態のリセット）/ `handleX`（入力処理）
- ボス本体の描画は `draw<Kind>BossBody`、雑魚敵の描画は `draw<Kind>Enemy`
- 種別分岐は文字列の `kind` / `type` / `hazard` フィールドを見て行う。ボス（`updateBoss` / `drawBoss`）と地形（hazard）は `if / else if` のディスパッチ、敵の生成（`spawnEnemy()`）だけは `switch` を使っている。既存の書き方に合わせればよく、どちらかに揃え直す必要はない
- コメントは日本語

## 禁止事項・注意点

- `version.js` を手動編集しない（自動生成ファイル）
- デバッグ用に `window.__t` 等のフックを一時的に追加してPlaywrightで検証するのはこのプロジェクトで確立した手法。ただし**コミット前に必ず削除する**
- `?debug=1` のデバッグモード（`DEBUG`定数）は通常プレイに一切影響しないことが前提。デバッグ専用の処理は必ず `if (DEBUG)` の中に置く
- 新しいステージ／ボス／敵／アイテムを追加するときは、既存のデータ駆動パターン（`STAGES`配列、`kind`ディスパッチ）に従う。既存の似た仕組み（別のボスの攻撃パターンや別ステージのhazard実装）を土台に流用できないか先に探す

詳細仕様（ステージ構成・ボス一覧・敵一覧・アイテム一覧・デバッグ手順など）は [`docs/spec.md`](docs/spec.md) を参照。全体を読む必要はなく、作業内容に対応する章だけ読めばよい。
